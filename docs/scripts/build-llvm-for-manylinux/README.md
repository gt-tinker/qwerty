First, run:

    $ ./build-manylinux-llvm.sh 19.1.6

Then the result will be in `io/llvm_mlir_rel_v19_1_6_x86_linux.tar.xz`.

Now you can run

    $ ./build-manylinux-qwerty.sh

Then the result will be in `io/qwerty-*.whl`.
